import QtQuick 2.11
import QtQuick.Controls 2.15
import QtQuick.Layouts 1.11

Popup {
    ColumnLayout {
        Label {
            text: "dialog"
        }
        // !!! intresting region
        EditBox { // EditBox.qml in the same directory, we can use it directly

        }
        // !!! intresting region
    }
}
